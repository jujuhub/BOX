#!/bin/bash
# Uploads all the .spc and .txt files to box for each material

for $MATERIAL in ${LIST_OF_FILE_EXT}
  check all *_test/ folders in Gd_testing/
  list each $MATERIAL out (all dates)
  separate into .spc and .txt lists
  upload to respective folders
